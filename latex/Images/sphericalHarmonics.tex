\documentclass[tikz]{standalone}
\usepackage{pgfplots}
\pgfplotsset{compat=1.18}
\def\samp{40}
\def\xsep{4.5cm}
\def\ysep{-6cm}
\begin{document}
\begin{tikzpicture}
  \pgfplotscolorbardrawstandalone[
    colormap/jet,
    point meta max=3,
    point meta min=-3,
    colorbar style={ytick={-3,3},
        xshift=6*\xsep - 0.25cm,
        yshift=-1cm,
        height=4cm,
        width=0.5cm,
        anchor=center,
      },
  ]
  \begin{axis}[%
      axis equal image,
      xmin=-1, xmax=1,
      ymin=-1, ymax=1,
      zmin=-1, zmax=1,
      % axis on top,
      xlabel = {$y$},
      ylabel = {$x$},
      zlabel = {$z$},
      ticks=none,
      grid,
      xtick={-0.5,0,0.5},
      xticklabels={},
      ytick={-0.5,0,0.5},
      yticklabels={},
      ztick={-0.5,0,0.5},
      zticklabels={},
      major grid style = {lightgray!25}, %Changes the color and stroke of the major grid.
      % axis line style={red},
      point meta min=-3,
      point meta max=3,
      colormap/jet,
      view/h=135,
      title = {$Y_{5,0}^\mathrm{c}$},
      title style = {scale=1.5},
      % % view/v=20,
      % scale uniformly strategy=units only,
      % y dir=reverse,
    ]
    \addplot3[% spherical harmonic (cosine) n = 5, m = 0
      opacity = 1,
      surf,
      shader=interp,
      point meta={sqrt(11)/8*(63*cos(v)^4-70*cos(v)^2+15)*cos(v)},
      z buffer = sort,
      samples = \samp,
      variable = u,
      variable y = v,
      domain = 0:360, % theta = u
      y domain = 0:180, % phi = v
    ]
    ({cos(u)*sin(v)}, {sin(u)*sin(v)}, {cos(v)});
  \end{axis}
  \begin{axis}[%
      xshift=\xsep,
      axis equal image,
      xmin=-1, xmax=1,
      ymin=-1, ymax=1,
      zmin=-1, zmax=1,
      % axis on top,
      xlabel = {$y$},
      ylabel = {$x$},
      zlabel = {$z$},
      ticks=none,
      grid,
      xtick={-0.5,0,0.5},
      xticklabels={},
      ytick={-0.5,0,0.5},
      yticklabels={},
      ztick={-0.5,0,0.5},
      zticklabels={},
      major grid style = {lightgray!25}, %Changes the color and stroke of the major grid.
      % axis line style={red},
      colormap/jet,
      point meta min=-3,
      point meta max=3,
      % enlargelimits=0.3,
      view/h=135,
      title = {$Y_{5,1}^\mathrm{c}$},
      title style = {scale=1.5},
      % % view/v=20,
      % scale uniformly strategy=units only,
      % y dir=reverse,
    ]
    \addplot3[% spherical harmonic (cosine) n = 5, m = 1
      opacity = 1,
      surf,
      shader=interp,
      point meta={sqrt(165)/8*sin(v)*(21*cos(v)^4-14*cos(v)^2+1)*cos(u)},
      z buffer = sort,
      samples = \samp,
      variable = u,
      variable y = v,
      domain = 0:360, % theta = u
      y domain = 0:180, % phi = v
    ]
    ({cos(u)*sin(v)}, {sin(u)*sin(v)}, {cos(v)});
  \end{axis}
  \begin{axis}[%
      xshift=2*\xsep,
      axis equal image,
      xmin=-1, xmax=1,
      ymin=-1, ymax=1,
      zmin=-1, zmax=1,
      % axis on top,
      xlabel = {$y$},
      ylabel = {$x$},
      zlabel = {$z$},
      ticks=none,
      grid,
      xtick={-0.5,0,0.5},
      xticklabels={},
      ytick={-0.5,0,0.5},
      yticklabels={},
      ztick={-0.5,0,0.5},
      zticklabels={},
      major grid style = {lightgray!25}, %Changes the color and stroke of the major grid.
      % axis line style={red},
      colormap/jet,
      point meta min=-3,
      point meta max=3,
      % enlargelimits=0.3,
      view/h=135,
      title = {$Y_{5,2}^\mathrm{c}$},
      title style = {scale=1.5},
      % % view/v=20,
      % scale uniformly strategy=units only,
      % y dir=reverse,
    ]
    \addplot3[% spherical harmonic (cosine) n = 5, m = 1
      opacity = 1,
      surf,
      shader=interp,
      point meta={sqrt(1155)/4*cos(v)*sin(v)^2(3*cos(v)^2-1)*cos(2*u)},
      z buffer = sort,
      samples = \samp,
      variable = u,
      variable y = v,
      domain = 0:360, % theta = u
      y domain = 0:180, % phi = v
    ]
    ({cos(u)*sin(v)}, {sin(u)*sin(v)}, {cos(v)});
  \end{axis}
  \begin{axis}[%
      xshift=3*\xsep,
      axis equal image,
      xmin=-1, xmax=1,
      ymin=-1, ymax=1,
      zmin=-1, zmax=1,
      % axis on top,
      xlabel = {$y$},
      ylabel = {$x$},
      zlabel = {$z$},
      ticks=none,
      grid,
      xtick={-0.5,0,0.5},
      xticklabels={},
      ytick={-0.5,0,0.5},
      yticklabels={},
      ztick={-0.5,0,0.5},
      zticklabels={},
      major grid style = {lightgray!25}, %Changes the color and stroke of the major grid.
      % axis line style={red},
      colormap/jet,
      point meta min=-3,
      point meta max=3,
      % enlargelimits=0.3,
      view/h=135,
      title = {$Y_{5,3}^\mathrm{c}$},
      title style = {scale=1.5},
      % % view/v=20,
      % scale uniformly strategy=units only,
      % y dir=reverse,
    ]
    \addplot3[% spherical harmonic (cosine) n = 5, m = 1
      opacity = 1,
      surf,
      shader=interp,
      point meta={(sin(v))^(3)*(9*cos(v)^2 - 1)*cos(3*u)*sqrt(770)/16},
      z buffer = sort,
      samples = \samp,
      variable = u,
      variable y = v,
      domain = 0:360, % theta = u
      y domain = 0:180, % phi = v
    ]
    ({cos(u)*sin(v)}, {sin(u)*sin(v)}, {cos(v)});
  \end{axis}
  \begin{axis}[%
      xshift=4*\xsep,
      axis equal image,
      xmin=-1, xmax=1,
      ymin=-1, ymax=1,
      zmin=-1, zmax=1,
      % axis on top,
      xlabel = {$y$},
      ylabel = {$x$},
      zlabel = {$z$},
      ticks=none,
      grid,
      xtick={-0.5,0,0.5},
      xticklabels={},
      ytick={-0.5,0,0.5},
      yticklabels={},
      ztick={-0.5,0,0.5},
      zticklabels={},
      major grid style = {lightgray!25}, %Changes the color and stroke of the major grid.
      % axis line style={red},
      colormap/jet,
      point meta min=-3,
      point meta max=3,
      % enlargelimits=0.3,
      view/h=135,
      title = {$Y_{5,4}^\mathrm{c}$},
      title style = {scale=1.5},
      % % view/v=20,
      % scale uniformly strategy=units only,
      % y dir=reverse,
    ]
    \addplot3[% spherical harmonic (cosine) n = 5, m = 1
      opacity = 1,
      surf,
      shader=interp,
      point meta={3*sin(v)^4*cos(v)*cos(4*u)*sqrt(385)/8},
      z buffer = sort,
      samples = \samp,
      variable = u,
      variable y = v,
      domain = 0:360, % theta = u
      y domain = 0:180, % phi = v
    ]
    ({cos(u)*sin(v)}, {sin(u)*sin(v)}, {cos(v)});
  \end{axis}
  \begin{axis}[%
      xshift=5*\xsep,
      axis equal image,
      xmin=-1, xmax=1,
      ymin=-1, ymax=1,
      zmin=-1, zmax=1,
      % axis on top,
      xlabel = {$y$},
      ylabel = {$x$},
      zlabel = {$z$},
      ticks=none,
      grid,
      xtick={-0.5,0,0.5},
      xticklabels={},
      ytick={-0.5,0,0.5},
      yticklabels={},
      ztick={-0.5,0,0.5},
      zticklabels={},
      major grid style = {lightgray!25}, %Changes the color and stroke of the major grid.
      % axis line style={red},
      colormap/jet,
      point meta min=-3,
      point meta max=3,
      % enlargelimits=0.3,
      view/h=135,
      title = {$Y_{5,5}^\mathrm{c}$},
      title style = {scale=1.5},
      % % view/v=20,
      % scale uniformly strategy=units only,
      % y dir=reverse,
    ]
    \addplot3[% spherical harmonic (cosine) n = 5, m = 1
      opacity = 1,
      surf,
      shader=interp,
      point meta={sqrt(154)*3/16*sin(v)^5*cos(5*u)},
      z buffer = sort,
      samples = \samp,
      variable = u,
      variable y = v,
      domain = 0:360, % theta = u
      y domain = 0:180, % phi = v
    ]
    ({cos(u)*sin(v)}, {sin(u)*sin(v)}, {cos(v)});
  \end{axis}
  \begin{axis}[%
      axis equal image,yshift=\ysep,
      xmin=-1, xmax=1,
      ymin=-1, ymax=1,
      zmin=-1, zmax=1,
      % axis on top,
      xlabel = {$y$},
      ylabel = {$x$},
      zlabel = {$z$},
      ticks=none,
      grid,
      xtick={-0.5,0,0.5},
      xticklabels={},
      ytick={-0.5,0,0.5},
      yticklabels={},
      ztick={-0.5,0,0.5},
      zticklabels={},
      major grid style = {lightgray!25}, %Changes the color and stroke of the major grid.
      % axis line style={red},
      point meta min=-3,
      point meta max=3,
      colormap/jet,
      view/h=135,
      title = {$Y_{5,0}^\mathrm{s}$},
      title style = {scale=1.5},
      % % view/v=20,
      % scale uniformly strategy=units only,
      % y dir=reverse,
    ]
    \addplot3[% spherical harmonic (cosine) n = 5, m = 0
      opacity = 1,
      surf,
      shader=interp,
      point meta={0},
      z buffer = sort,
      samples = \samp,
      variable = u,
      variable y = v,
      domain = 0:360, % theta = u
      y domain = 0:180, % phi = v
    ]
    ({cos(u)*sin(v)}, {sin(u)*sin(v)}, {cos(v)});
  \end{axis}
  \begin{axis}[%
      xshift=\xsep,yshift=\ysep,
      axis equal image,
      xmin=-1, xmax=1,
      ymin=-1, ymax=1,
      zmin=-1, zmax=1,
      % axis on top,
      xlabel = {$y$},
      ylabel = {$x$},
      zlabel = {$z$},
      ticks=none,
      grid,
      xtick={-0.5,0,0.5},
      xticklabels={},
      ytick={-0.5,0,0.5},
      yticklabels={},
      ztick={-0.5,0,0.5},
      zticklabels={},
      major grid style = {lightgray!25}, %Changes the color and stroke of the major grid.
      % axis line style={red},
      colormap/jet,
      point meta min=-3,
      point meta max=3,
      % enlargelimits=0.3,
      view/h=135,
      title = {$Y_{5,1}^\mathrm{s}$},
      title style = {scale=1.5},
      % % view/v=20,
      % scale uniformly strategy=units only,
      % y dir=reverse,
    ]
    \addplot3[% spherical harmonic (cosine) n = 5, m = 1
      opacity = 1,
      surf,
      shader=interp,
      point meta={sqrt(165)/8*sin(v)*(21*cos(v)^4-14*cos(v)^2+1)*sin(u)},
      z buffer = sort,
      samples = \samp,
      variable = u,
      variable y = v,
      domain = 0:360, % theta = u
      y domain = 0:180, % phi = v
    ]
    ({cos(u)*sin(v)}, {sin(u)*sin(v)}, {cos(v)});
  \end{axis}
  \begin{axis}[%
      xshift=2*\xsep,yshift=\ysep,
      axis equal image,
      xmin=-1, xmax=1,
      ymin=-1, ymax=1,
      zmin=-1, zmax=1,
      % axis on top,
      xlabel = {$y$},
      ylabel = {$x$},
      zlabel = {$z$},
      ticks=none,
      grid,
      xtick={-0.5,0,0.5},
      xticklabels={},
      ytick={-0.5,0,0.5},
      yticklabels={},
      ztick={-0.5,0,0.5},
      zticklabels={},
      major grid style = {lightgray!25}, %Changes the color and stroke of the major grid.
      % axis line style={red},
      colormap/jet,
      point meta min=-3,
      point meta max=3,
      % enlargelimits=0.3,
      view/h=135,
      title = {$Y_{5,2}^\mathrm{s}$},
      title style = {scale=1.5},
      % % view/v=20,
      % scale uniformly strategy=units only,
      % y dir=reverse,
    ]
    \addplot3[% spherical harmonic (cosine) n = 5, m = 1
      opacity = 1,
      surf,
      shader=interp,
      point meta={sqrt(1155)/4*cos(v)*sin(v)^2(3*cos(v)^2-1)*sin(2*u)},
      z buffer = sort,
      samples = \samp,
      variable = u,
      variable y = v,
      domain = 0:360, % theta = u
      y domain = 0:180, % phi = v
    ]
    ({cos(u)*sin(v)}, {sin(u)*sin(v)}, {cos(v)});
  \end{axis}
  \begin{axis}[%
      xshift=3*\xsep,yshift=\ysep,
      axis equal image,
      xmin=-1, xmax=1,
      ymin=-1, ymax=1,
      zmin=-1, zmax=1,
      % axis on top,
      xlabel = {$y$},
      ylabel = {$x$},
      zlabel = {$z$},
      ticks=none,
      grid,
      xtick={-0.5,0,0.5},
      xticklabels={},
      ytick={-0.5,0,0.5},
      yticklabels={},
      ztick={-0.5,0,0.5},
      zticklabels={},
      major grid style = {lightgray!25}, %Changes the color and stroke of the major grid.
      % axis line style={red},
      colormap/jet,
      point meta min=-3,
      point meta max=3,
      % enlargelimits=0.3,
      view/h=135,
      title = {$Y_{5,3}^\mathrm{s}$},
      title style = {scale=1.5},
      % % view/v=20,
      % scale uniformly strategy=units only,
      % y dir=reverse,
    ]
    \addplot3[% spherical harmonic (cosine) n = 5, m = 1
      opacity = 1,
      surf,
      shader=interp,
      point meta={(sin(v))^(3)*(9*cos(v)^2 - 1)*sin(3*u)*sqrt(770)/16},
      z buffer = sort,
      samples = \samp,
      variable = u,
      variable y = v,
      domain = 0:360, % theta = u
      y domain = 0:180, % phi = v
    ]
    ({cos(u)*sin(v)}, {sin(u)*sin(v)}, {cos(v)});
  \end{axis}
  \begin{axis}[%
      xshift=4*\xsep,yshift=\ysep,
      axis equal image,
      xmin=-1, xmax=1,
      ymin=-1, ymax=1,
      zmin=-1, zmax=1,
      % axis on top,
      xlabel = {$y$},
      ylabel = {$x$},
      zlabel = {$z$},
      ticks=none,
      grid,
      xtick={-0.5,0,0.5},
      xticklabels={},
      ytick={-0.5,0,0.5},
      yticklabels={},
      ztick={-0.5,0,0.5},
      zticklabels={},
      major grid style = {lightgray!25}, %Changes the color and stroke of the major grid.
      % axis line style={red},
      colormap/jet,
      point meta min=-3,
      point meta max=3,
      % enlargelimits=0.3,
      view/h=135,
      title = {$Y_{5,4}^\mathrm{s}$},
      title style = {scale=1.5},
      % % view/v=20,
      % scale uniformly strategy=units only,
      % y dir=reverse,
    ]
    \addplot3[% spherical harmonic (cosine) n = 5, m = 1
      opacity = 1,
      surf,
      shader=interp,
      point meta={3*sin(v)^4*cos(v)*sin(4*u)*sqrt(385)/8},
      z buffer = sort,
      samples = \samp,
      variable = u,
      variable y = v,
      domain = 0:360, % theta = u
      y domain = 0:180, % phi = v
    ]
    ({cos(u)*sin(v)}, {sin(u)*sin(v)}, {cos(v)});
  \end{axis}
  \begin{axis}[%
      xshift=5*\xsep,yshift=\ysep,
      axis equal image,
      xmin=-1, xmax=1,
      ymin=-1, ymax=1,
      zmin=-1, zmax=1,
      % axis on top,
      xlabel = {$y$},
      ylabel = {$x$},
      zlabel = {$z$},
      ticks=none,
      grid,
      xtick={-0.5,0,0.5},
      xticklabels={},
      ytick={-0.5,0,0.5},
      yticklabels={},
      ztick={-0.5,0,0.5},
      zticklabels={},
      major grid style = {lightgray!25}, %Changes the color and stroke of the major grid.
      % axis line style={red},
      colormap/jet,
      point meta min=-3,
      point meta max=3,
      % enlargelimits=0.3,
      view/h=135,
      title = {$Y_{5,5}^\mathrm{s}$},
      title style = {scale=1.5},
      % % view/v=20,
      % scale uniformly strategy=units only,
      % y dir=reverse,
    ]
    \addplot3[% spherical harmonic (cosine) n = 5, m = 1
      opacity = 1,
      surf,
      shader=interp,
      point meta={sqrt(154)*3/16*sin(v)^5*sin(5*u)},
      z buffer = sort,
      samples = \samp,
      variable = u,
      variable y = v,
      domain = 0:360, % theta = u
      y domain = 0:180, % phi = v
    ]
    ({cos(u)*sin(v)}, {sin(u)*sin(v)}, {cos(v)});
  \end{axis}
\end{tikzpicture}
\end{document}